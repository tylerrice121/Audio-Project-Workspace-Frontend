import { Helmet } from "react-helmet";

const Home = (props) => {
    return (
        <>
        <Helmet>
            <title>Welcome to APW</title>
        </Helmet>
        <main>
            <h1>Home</h1>
        </main>

        </>
    );
};

export default Home;